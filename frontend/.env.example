# Next.js client/server env (copy to frontend/.env.local)
# Omit NEXT_PUBLIC_API_BASE_URL to use the built-in /api/maweshi-proxy/chat rewrite (no CORS issues).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
